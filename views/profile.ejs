<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="w-full mi-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2  inline-block mb-5" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-5">Hey ! <%= user.name %> 👋</h3>

        <p class="mb-5">Create Post</p>
        <form action="/post" method="post">
            <textarea class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                placeholder="Write Your post" name="content" id="" cols="30" rows="10"></textarea>
            <input type="submit" value="Create New Post" class="px-3 py-2 bg-blue-500 block rounded-md mt-4 w-40">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400 ">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){%>

                 
                <div class="post mb-3 w-1/3 p-4 border-[1px] border-zinc-700 rounded-md bg-zinc-800">
                    <h4 class="text-blue-500 mb-2"><%=user.username%></h4>
                    <p class="text-sm tracking tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                    <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "like" : "Unlike" %>

                        </a>
                        <a class="text-blue-500" href="/edit/<%= post._id %>">Edit</a>


                    </div>

                </div>
                <%  }) %>

            </div>
        </div>
    </div>
</body>

</html> -->


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap');

        body {
            font-family: 'Poppins', sans-serif;
        }

        .fade-in {
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .bg-gradient-to-br {
            background-image: linear-gradient(to bottom right, #4a90e2, #9013fe);
        }
    </style>
</head>

<body class="bg-zinc-900 text-white p-10">
    <div class="w-full min-h-screen">
        <div class="flex w-full justify-end">
            <a class="bg-red-500 hover:bg-red-600 transition duration-300 rounded-md px-3 py-2 inline-block mb-5 fade-in" href="/logout">Logout</a>
        </div>
        <h3 class="text-3xl mb-5 fade-in">Hey! <%= user.name %> 👋</h3>

        <p class="mb-5 fade-in">Create Post</p>
        <form action="/post" method="post" class="fade-in">
            <textarea class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-purple-500 transition duration-300"
                placeholder="Write Your post" name="content" id="" cols="30" rows="10"></textarea>
            <input type="submit" value="Create New Post" class="px-3 py-2 bg-purple-500 hover:bg-purple-600 transition duration-300 block rounded-md mt-4 w-40">
        </form>
        <div class="posts mt-20 fade-in">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){%>
                <div class="post mb-3 w-1/3 p-4 border-[1px] border-zinc-700 rounded-md bg-zinc-800 hover:shadow-lg transition duration-300 fade-in">
                    <h4 class="text-teal-400 mb-2"><%=user.username%></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                    <div class="btns flex gap-4 mt-5">
                        <a class="text-purple-500 hover:underline transition duration-300" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a class="text-purple-500 hover:underline transition duration-300" href="/edit/<%= post._id %>">Edit</a>
                    </div>
                </div>
                <%  }) %>
            </div>
        </div>
    </div>
</body>

</html>

